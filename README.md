# eRAP PDF Parser & Automation System

Комплексная система для автоматизации работы с порталом eRAP и парсинга PDF-документов штрафов от Департамента полиции Алматинской области.

## 📋 Содержание

- [Обзор проекта](#обзор-проекта)
- [Архитектура системы](#архитектура-системы)
- [Установка и настройка](#установка-и-настройка)
- [Использование бота eRAP](#использование-бота-erap)
- [API для работы с штрафами](#api-для-работы-со-штрафами)
- [Парсер PDF документов](#парсер-pdf-документов)
- [Безопасность](#безопасность)
- [Примеры использования](#примеры-использования)
- [Часто задаваемые вопросы](#часто-задаваемые-вопросы)
- [Вклад в проект](#вклад-в-проект)

## 🎯 Обзор проекта

Система состоит из двух основных компонентов:

### 1. Автоматизация входа в eRAP
Бот для автоматической аутентификации на портале eRAP (https://erap-public.kgp.kz) с использованием электронной цифровой подписи (ЭЦП) через NCALayer.

**Основные функции:**
- Автоматический вход в личный кабинет
- Интерактивная калибровка координат GUI элементов
- Умный ввод пароля в диалог NCALayer
- Автоматический выбор сертификата
- Поддержка кроссплатформенности (macOS, Linux, Windows)

### 2. API для управления штрафами
RESTful API для загрузки, хранения и управления данными о штрафах из PDF документов.

**Основные функции:**
- Загрузка и парсинг PDF документов штрафов
- Хранение в базе данных SQLite
- Фильтрация и поиск по различным параметрам
- Отслеживание статуса оплаты и скидок

## 🚀 Установка и настройка

### Системные требования

- Python 3.8+
- Операционная система: macOS / Linux / Windows
- Установленный NCALayer для работы с ЭЦП
- Chromium браузер (устанавливается автоматически)

Создайте файл `.env` в корне проекта:
```env
API_TOKEN=
CERT_PATH=/Users/.../.../cert.p12
CERT_PASSWORD=password
NCALAYER_URL=wss://127.0.0.1:13579
DOWNLOAD_DIR=traffic_fines/downloads
LOG_DIR=traffic_fines/logs
HEADLESS=false
TIMEOUT=30000
BASE_URL=https://erap-public.kgp.kz/#/login
LOG_LEVEL=INFO
```
### Генерация безопасного токена

Для генерации криптографически безопасного токена:

```bash
python security_layer.py
```

Эта команда сгенерирует токен и выведет его для добавления в `.env` файл.

## 🤖 Использование бота eRAP

### Первый запуск (калибровка)

При первом запуске бот перейдет в режим калибровки для определения координат элементов интерфейса NCALayer:

```bash
python erap_bot.py
```

**Процесс калибровки:**
1. Бот откроет браузер и перейдет на сайт eRAP
2. Нажмет кнопку входа и выберет вход через сертификат
3. Появится диалог NCALayer для ввода пароля
4. Следуйте инструкциям на экране для калибровки:
   - Кликните на поле ввода пароля когда prompted
   - Введите пароль вручную и нажмите Enter
   - Кликните на сертификат в списке когда prompted

Координаты сохранятся в файле `coordinates.json` для последующих запусков.

### Повторные запуски

После калибровки бот будет работать автоматически:

```bash
python erap_bot.py
```

Бот выполнит полный цикл аутентификации и оставит браузер открытым для дальнейшей работы.

## 🌐 API для работы со штрафами

### Запуск API сервера

```bash
python api.py
```

Сервер будет доступен по адресу: `http://localhost:8000`

### Документация API

- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

### Аутентификация

Все эндпоинты требуют аутентификации через Bearer токен:

```bash
curl -X GET "http://localhost:8000/fines" \
  -H "Authorization: Bearer YOUR_API_TOKEN"
```

### Основные эндпоинты

#### 1. Загрузка PDF документа

```bash
curl -X POST "http://localhost:8000/fines/upload" \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -F "file=@fine_document.pdf"
```

#### 2. Получение списка штрафов

```bash
curl -X GET "http://localhost:8000/fines" \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -G -d "license_plate=ABC123" \
  -d "violation_date_from=2024-01-01T00:00:00Z" \
  -d "discount_available_only=true"
```

#### 3. Получение информации о штрафе

```bash
curl -X GET "http://localhost:8000/fines/{fine_id}" \
  -H "Authorization: Bearer YOUR_API_TOKEN"
```

#### 4. Отметка об оплате

```bash
curl -X PATCH "http://localhost:8000/fines/{fine_id}/mark-paid" \
  -H "Authorization: Bearer YOUR_API_TOKEN"
```

#### 5. Проверка состояния системы

```bash
curl -X GET "http://localhost:8000/health"
```

## 📄 Парсер PDF документов

### Поддерживаемые документы

Парсер обрабатывает PDF документы штрафов с поддержкой двух языков (казахский и русский).

### Извлекаемые данные

| Поле | Описание | Пример |
|------|----------|--------|
| `prescription_number` | Номер предписания | "123456789012345" |
| `license_plate` | Госномер автомобиля | "ABC123" |
| `vehicle_certificate` | Номер СРТС | "123ABC456" |
| `vehicle_make_model` | Марка и модель | "TOYOTA CAMRY" |
| `violation_datetime` | Дата и время нарушения | "2024-01-15 14:30" |
| `violation_location` | Место нарушения | "ул. Абая, д. 123" |
| `detected_speed` | Зафиксированная скорость | 85.0 |
| `allowed_speed` | Разрешенная скорость | 60.0 |
| `fine_amount` | Сумма штрафа | 15000.0 |
| `discounted_amount` | Сумма со скидкой | 7500.0 |

### Использование парсера

```python
from pdf_parser import TrafficFinePDFParser
from pathlib import Path

# Создание экземпляра парсера
parser = TrafficFinePDFParser()

# Парсинг PDF файла
pdf_path = Path("fine_document.pdf")
parsed_data = parser.parse_file(pdf_path)

print(f"Номер предписания: {parsed_data['prescription_number']}")
print(f"Госномер: {parsed_data['license_plate']}")
print(f"Сумма штрафа: {parsed_data['fine_amount']} тенге")
```

## ❓ Часто задаваемые вопросы

### Вопрос: Бот не может найти элементы интерфейса NCALayer

**Ответ:** Убедитесь, что:
1. NCALayer установлен и запущен
2. Сертификат ЭЦП установлен в системе
3. Выполнена калибровка координат при первом запуске
4. Окно NCALayer не перекрыто другими приложениями

### Вопрос: API возвращает ошибку 401 Unauthorized

**Ответ:** Проверьте:
1. Токен указан правильно в заголовке Authorization
2. Токен не истек и соответствует настройкам в .env файле
3. Формат заголовка: `Authorization: Bearer YOUR_TOKEN`

### Вопрос: Парсер не извлекает данные из PDF

**Ответ:** Убедитесь, что:
1. PDF документ соответствует формату штрафов Публичного сектора административных производств РК
2. Документ содержит текст (не является сканированным изображением)
3. Язык документа поддерживается (казахский или русский)

### Вопрос: Как изменить время ожидания операций?

**Ответ:** Измените параметр `timeout` в классе [`ConfigDict`](erap_bot.py:20):

```python
class ConfigDict(BaseSettings):
    timeout: int = 120000  # 120 секунд
```

### Вопрос: Можно ли запустить бота в фоновом режиме?

**Ответ:** Да, установите параметр `headless: True` в конфигурации:

```python
config = ConfigDict(
    cert_password="your_password",
    headless=True  # Фоновый режим
)
```

### Вопрос: Как создать резервную копию базы данных?

**Ответ:** База данных SQLite хранится в файле `traffic_fines/fines.db`. Создайте копию этого файла:

```bash
cp traffic_fines/fines.db backup/fines_$(date +%Y%m%d).db
```

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.